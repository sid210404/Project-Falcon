import pandas as pd
import plotly.graph_objects as go


def add_indicator_layer(
    fig: go.Figure,
    df: pd.DataFrame,
) -> None:
    """
    Indicator layer.

    Will be implemented in the next task.
    """

    return